<div id="roomselector_blacklayer" />
<div id="roomselector_formarea">

	<select id="roomselector_selector">
		 <option value="{{roomid}}">{{roomname}}
		 {{#nodes}}
			<option value="{{id}}">{{name}}
		{{/nodes}}
		<option value="addnewroom">*** add new room **
	</select>
	<button id="roomselector_createnew">change room</button>
	<button id="roomselector_cancel">cancel</button>
</div>

